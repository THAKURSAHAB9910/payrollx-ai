# Database Configuration Properties
# Options: h2, mysql, postgresql
db.type=h2

# --- H2 Database (In-Memory, Default for easy run) ---
db.url=jdbc:h2:mem:payrollxdb;DB_CLOSE_DELAY=-1;MODE=MySQL
db.username=sa
db.password=

# --- MySQL Configuration (Uncomment to use) ---
# db.type=mysql
# db.url=jdbc:mysql://localhost:3306/payrollx?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# db.username=root
# db.password=yourpassword

# --- PostgreSQL Configuration (Uncomment to use) ---
# db.type=postgresql
# db.url=jdbc:postgresql://localhost:5432/payrollx
# db.username=postgres
# db.password=yourpassword
